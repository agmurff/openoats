PySide6~=6.7.0
qasync~=0.27.0
sounddevice~=0.4.7
pyaudiowpatch~=0.2.12
faster-whisper~=1.1.0
silero-vad~=5.1.0
# CPU torch (default). For GPU: pip install torch~=2.3.0 --index-url https://download.pytorch.org/whl/cu121
torch~=2.3.0
torchaudio~=2.3.0  # MUST match torch version (resolver otherwise grabs an ABI-incompatible build)
requests~=2.32     # transitive dep faster-whisper imports but no longer pulls in automatically
httpx~=0.27.0
keyring~=25.2.0
pywin32>=306        # Outlook COM calendar matching (classic Outlook)
numpy~=1.26.0
scipy~=1.13.0
